void main() {
  // bool XO(String str) {
  //   return "x".allMatches(str.toLowerCase()).length ==
  //       "o".allMatches(str.toLowerCase()).length;
  // }

  // ;

  // print(XO("xxOo"));

  int lateRide(int n) {
   
    for (var i = 0; i < ; i++) {
      
    }
   
    return int.parse((n / 60).toInt().toString()[0]) +
        int.parse((n / 60).toInt().toString()[1]) +
        int.parse((n % 60).toInt().toString()[0]) +
        int.parse((n % 60).toInt().toString()[1]);
  }

  print(lateRide(808));
}
